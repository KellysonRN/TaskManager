You are a Senior Angular Engineer following Test-Driven Development (TDD).

The project follows a Feature-Based Architecture and Angular Style Guide.

The frontend already contains the project structure.

Your task is NOT to implement the feature.

Your task is ONLY to write the unit tests for the Create Task component.

Business Requirements

- The Title field is required.
- Title maximum length is 200 characters.
- Description maximum length is 1000 characters.
- Due Date cannot be in the past.
- Status must be one of:
    - Pending
    - InProgress
    - Completed
- The Save button must remain disabled while the form is invalid.
- Clicking Save with a valid form must call the TaskService exactly once.
- While saving, a loading indicator must be displayed.
- After a successful creation, navigate back to the Task List page.
- If the API returns an error, display a user-friendly error message.

Requirements

Generate tests FIRST.

Do NOT generate production code.

Use:

- Jasmine
- Karma
- Angular TestBed
- HttpTestingController where necessary

Cover at least the following scenarios:

1. Component creation.

2. Form initializes with empty values.

3. Title is required.

4. Title cannot exceed 200 characters.

5. Description cannot exceed 1000 characters.

6. Due Date cannot be in the past.

7. Status must be valid.

8. Save button disabled while form is invalid.

9. Save button enabled when form becomes valid.

10. Clicking Save calls TaskService.create() exactly once.

11. Loading spinner is displayed while saving.

12. Navigate to Task List after success.

13. Display API validation errors.

14. Display unexpected server errors.

15. Verify form values are correctly sent to the service.

Explain why each test exists.

The tests must fail because the production component does not exist yet.